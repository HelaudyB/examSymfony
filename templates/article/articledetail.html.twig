{% extends 'base.html.twig' %}
{% block body %}
    <div class="example-wrapper">
        <h1> {{ article.title }}</h1>
        <img src="{{ article.image }}">
        <p>{{ article.content }}</p>
        <h6>Article écrit le {{ article.created_at }} par {{ article.author }} .</h6>



    </div>
{% endblock %}